<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.jmj.trade</groupId>
    <artifactId>trading-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>trading-backend</name>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-json</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>3.12.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.3.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!--
                        Two @SpringBootApplication classes exist: the real app, and
                        com.jmj.trade.migrate.MigrateOnlyApplication (a one-shot Flyway-only
                        entrypoint for staging/production deploys). Pin the packaged JAR's
                        default Start-Class so repackage doesn't fail on ambiguity; the
                        migrate entrypoint is still on the classpath and is launched via
                        -Dloader.main=com.jmj.trade.migrate.MigrateOnlyApplication.
                    -->
                    <mainClass>com.jmj.trade.TradingBackendApplication</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!--
                        ~18 distinct @SpringBootTest configurations in this suite each get
                        their own cached ApplicationContext, and most set no Hikari pool
                        size — Hikari's own default (10 max, minIdle = max) means most of
                        those contexts hold 10 idle connections apiece against the one
                        shared Testcontainers Postgres instance from PostgresIntegrationTest,
                        for as long as they stay in Spring's test context cache. Measured
                        peak before this change: ~150 simultaneous connections, well past
                        Postgres's own default max_connections=100 — the direct cause of the
                        "too many clients already" CI failure this fixes.

                        A handful of classes already cap this per-class (grep
                        "hikari.maximum-pool-size" under src/test); this generalizes that
                        same pattern as the default instead of leaving every other class on
                        Hikari's unconstrained default. @SpringBootTest(properties = ...) on
                        an individual class still wins over a system property, so those
                        existing per-class overrides are unaffected.

                        Deliberately NOT setting minimum-idle here: it doesn't change the
                        exhaustion ceiling (still bounded by maximum-pool-size alone), only
                        how many connections stay pre-warmed. Setting it to 0 forced every
                        checkout to re-establish a physical connection, which cost enough
                        latency under GitHub Actions' runner to blow a test's own 500ms
                        outbound HTTP timeout budget (PortfolioAnalysisWorkflowIntegrationTest)
                        — a real regression caught on the first CI run of this fix. Leaving
                        minimum-idle unset lets Hikari default it to maximum-pool-size, so
                        connections stay pre-warmed while the peak count is still capped.
                    -->
                    <systemPropertyVariables>
                        <spring.datasource.hikari.maximum-pool-size>2</spring.datasource.hikari.maximum-pool-size>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
