**/target/
**/.pytest_cache/
**/__pycache__/
**/.venv/
*.py[cod]
.env
.env.*
!.env.example
!.env.mock.example
!.env.staging.example
backups/
*.dump
.staging-drill-secrets*
client_secret*.json

# Agent tooling state and generated artifacts. Never commit: these land inside the
# source tree (including src/main/resources, which Maven copies into target/classes
# and the container image) and would otherwise pollute builds and image layers.
.omc/
.omo/
graphify-out/
.superpowers/
AGENTS.md

# task-observer (로그는 ~/.claude/projects/ 로 가야 정상, 방어용)
skill-observations/
